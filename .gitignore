.venv/
__pycache__/
*.py[cod]
jinx_recruiting.db
static/uploads/
.env

# HawkEye generated analysis snapshots (VS Code extension tooling artifacts)
.hawkeye/

# Scratch pages fetched during research runs (agents save HTML to the repo root)
/*.html
_tmp/
# Stray research files agents occasionally write to data/ root instead of subdirs
/data/*.json
